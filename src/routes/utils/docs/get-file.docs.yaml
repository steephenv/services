/utils/get-image-url?fileName=xxxxxxxxxxxxxx:
  get:
    security:
    - Bearer: []
    summary: api for fetch imgUrl.
    tags:
    - Utils
    description: api for fetch imgUrl.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: fileName
      description: ''
      in: query
      required: true    
    responses:
      200:
        description: "successful attempt, returns `success message`"